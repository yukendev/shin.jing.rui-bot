# 曜日のテスト
name: Day test
on:
  schedule:
    - cron: "15 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Send message
        uses: actions/setup-node@v3
        with:
          node-version: "20.x"
      - run: npm run day-test
